instance_specs: 
  max_price_usd_hour: 0.2

work_dir: "work_dir"

setup:
  run: 
    - "sudo apt-get update && sudo apt-get install -y python3 python3-venv python3-pip zip unzip"
    - "pip install tqdm"
 
task:
  run:
  - "python3 pytorch_simulate.py"
